<div class="span12">
	<form class="form-search" >
		<input data-ng-model="searchText" placeholder="Rechercher" type="text" class="input-medium search-query" data-ng-change="search()">
		<input data-typeahead="address for address in getLocation($viewValue)" data-typeahead-loading="loadingLocations"
			data-ng-model="searchPlace" placeholder="Ou ?" type="text" class="input-medium search-query"
			data-typeahead-editable='false'>
		<i ng-show="loadingLocations" class="icon-refresh"></i>
	</form>
	{# Affichage des catégories de donnée #}
	{% verbatim %}
	<ul class="inline">
		<li>Categorie</li>
		<li>Afficher</li>
	</ul>
	{% endverbatim %}
</div>
{# Affichage des données avec leur titre un apercu de leur descriptif et leur image si il y en a une #}
<div class="span12">
	<div class="row">
		<div class="span12 horizontal-data-bloc" data-ng-repeat="dataset in datasets" data-ng-click="goto(dataset)">
			{% verbatim %}
				<h5 class="horizontal-data-bloc-title">{{ dataset.name }}</h5>
				<p>
					 {{ dataset.desc | limitTo:300 }}...
				</p>
				<div class="horizontal-data-bloc-bottom">
					<ul class="meta-post">
						<li><i class="icon-copy"></i> Jeux de donnée</li>
						<li data-ng-if="dataset.lastUpdate"><a tooltip='Mise à jour'><i class="icon-calendar"></i> {{ dataset.lastUpdate }}</a></li>
						<li><a tooltip='Auteur'><i class="icon-user"></i> {{ dataset.user }}</a></li>
						<li><a tooltip='Localisation'><i class="icon-pushpin"></i> {{ dataset.location }}</a></li>
						<li><a tooltip='Couverture'><i class="icon-globe"></i> {{ dataset.couverture }}</a></li>
					</ul>
				</div>
			{% endverbatim %}
		</div>
	</div>
</div>
{# Affichage pagination #}
<div id="pagination" class="span12">
	<span class="all">Page 1 of 3</span>
	<span class="current">1</span>
	<a href="#" class="inactive">2</a>
	<a href="#" class="inactive">3</a>
</div>