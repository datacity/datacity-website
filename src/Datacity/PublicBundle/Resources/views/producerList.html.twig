{% extends 'DatacityPublicBundle:Template:pagebody.html.twig' %}

{# Ici, on configure les parametre de base de la page #}
{% block title %}Datacity -- Producteurs{% endblock %}
{% block pagetitle %}Producteurs{% endblock %}
{% block pagename %}Producteurs{% endblock %}

{% block pagecontent %}
	<section id="content">
	<div class="container">
		<div class="row">
			<div class="span12">
				<div class="row">
					<section id="projects" style='min-height: 550px;'>
					<ul id="thumbs" class="portfolio">
					{# Boucle for de creation des block correspondant aux producteurs en base de donnee #}
					{% for producer in producers %}
						<!-- Item Project and Filter Name -->
						<li class="item-thumbs span4">
						<h5>{{ producer.name }}</h5>
						<!-- Fancybox - Gallery Enabled - Title - Full Image -->
						<a class="hover-wrap fancybox" data-fancybox-group="gallery" title="{{ producer.name }}" href="{{ path('datacity_public_producer_detail', {'slug' : producer.slug }) }}">
						<span class="overlay-img"></span>
						<span class="overlay-img-thumb font-icon-plus"></span>
						</a>
						<!-- Thumb Image and Description -->
						<img style="width: 370px; height:166px;" src="{{ producer.image.url }}" alt="{{ producer.image.alt }}">
						</li>
					{% endfor %}
					</ul>
					</section>
				</div>
			</div>
		</div>
	</div>
	</section>


{% endblock %}
