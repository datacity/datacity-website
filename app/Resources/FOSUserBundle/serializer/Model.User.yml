# app/Resources/FOSUserBundle/serializer/Model.User.yml
FOS\UserBundle\Model\User:
    exclusion_policy: ALL
    properties:
        username:
            type: string
            expose: true
            groups: [list, datasetShow]
        usernameCanonical:
            type: string
            expose: true
            groups: [list, datasetShow]

